@model IEnumerable<lz523915_MIS4200.Models.VisitDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pet.petBreed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Treatment.treatmentDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.visitPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pet.petBreed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Treatment.treatmentDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.visitPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.visitDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.visitDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.visitDetailId })
        </td>
    </tr>
}

</table>
